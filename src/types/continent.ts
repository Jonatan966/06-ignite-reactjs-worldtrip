export default interface Continent {
  image: {
    url: string,
  },
  name: string,
  description: string,
  summary: string,
  slug: string,
  countries_amount: number,
  languages_amount: number,
}
